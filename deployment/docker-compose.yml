# Phase 5 will finalize Traefik labels and production service settings.
services:
  dashboard:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    env_file:
      - ../.env
    ports:
      - "8501:8501"
    volumes:
      - ../data:/app/data
      - ../reports:/app/reports
